openinference-instrumentation-openai
aiohttp

# EUR-Lex MCP Server Evaluation Dependencies
# Install with: pip install -r requirements.txt

sentencepiece
sentence_transformers

# Core dependencies
requests>=2.31.0
tqdm>=4.66.0
aiohttp>=3.9.0

# Data handling
datasets>=2.16.0

# Query generation (Phase 2)
transformers>=4.36.0
torch>=2.1.0
sentence-transformers>=2.2.2

# Cross-encoder scoring (Phase 3)
# Already included in sentence-transformers

# LLM-as-judge (Phase 4)
# Requires Ollama running locally: ollama pull llama3.1:70b
langchain-community>=0.0.10
# Optional: RAGAS for RAG evaluation
ragas>=0.1.0

# Metrics computation (Phase 6)
ranx>=0.3.16
scikit-learn>=1.3.0

# Experiment tracking
mlflow>=2.9.0

# Human annotation (Phase 5)
# Optional: Label Studio for annotation interface
# label-studio>=1.10.0

# Development and testing
pytest>=7.4.0
pytest-asyncio>=0.21.0

# Optional: for faster JSON handling
orjson>=3.9.0
